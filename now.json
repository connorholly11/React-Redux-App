{
    "version": 2,
    "name": "apiredux",
    "builds": [{ "src": "apiredux/package.json", "use": "@now/static-build", "config": {"distDir": "build"} }],
    "routes": [
        { "src": "/(.*)", "dest": "apiredux/$1" }
    ]
}