export const initialState = {
    players: null,
    isFetching: false,
    error: ''
}

export const PlayerReducer = (state = initialState, action) => {
    switch(action.type){

        default:
            return state
    }

}